@model List<GaragePortal.Razor.Models.CarDTO>
@{
    ViewData["Title"] = "Customer Vehicles & Services";
}
<h2>My Vehicles and Service History</h2>
@if (Model.Count != 0)
{
    foreach (var car in Model)
    {
        <div class="card my-4">
            <div class="card-header bg-primary text-white">
                <strong>Car License Plate:</strong> @car.LicensePlate
            </div>
            <div class="card-body">
                @if (car.Repairs == null || !car.Repairs.Any())
                {
                    <p class="text-warning">This car has no service history.</p>
                }
                else
                {
                    <h5>Service History</h5>
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Service Date</th>
                                <th>Completion Date</th>
                                <th>Technician</th>
                                <th>Work to be done</th>
                                <th>Status</th>
                                <th>Hours</th>
                                <th>Total Cost</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var service in car.Repairs)
                            {
                                if (service.RepairStatus == "Pending")
                                {
                                    <tr class="table-warning">
                                        <td>@service.RepairDate.ToShortDateString()</td>
                                        <td colspan="2" class="text-center text-muted">Service is still in progress</td>
                                        <td>@(string.IsNullOrEmpty(service.EstimatedWorkDescription) ? "Not specified" : service.EstimatedWorkDescription)</td>
                                        <td><strong>@service.RepairStatus</strong></td>
                                        <td>@(service.Hours.ToString("F1") ?? "N/A")</td>
                                        <td>@(service.RepairCost.HasValue ? "$" + service.RepairCost.Value.ToString("F2") : "Pending")</td>
                                    </tr>
                                }
                                else if (service.RepairStatus == "Completed")
                                {
                                    <tr class="table-success">
                                        <td>@service.RepairDate.ToShortDateString()</td>
                                        <td>
                                            @if (service.FinishDate.HasValue)
                                            {
                                                @service.FinishDate.Value.ToShortDateString()
                                            }
                                            else
                                            {
                                                <span class="text-danger">N/A</span>
                                            }
                                        </td>
                                        <td>@service.TechnicianName</td>
                                        <td>@service.RepairDescription</td>
                                        <td><span class="badge bg-success">@service.RepairStatus</span></td>
                                        <td>@service.Hours.ToString("F1") hours</td>
                                        <td>
                                            <strong>@(service.RepairCost.HasValue ? "$" + service.RepairCost.Value.ToString("F2") : "N/A")</strong>
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>
    }
}
else
{
    <div class="alert alert-info">
        <h4>No Vehicles Found</h4>
        <p>You don't have any vehicles registered yet. Please contact the administrator to add your vehicle.</p>
    </div>
}